/// Wallet feature package — public API.
///
/// Add `WalletFeature()` to `buildFeatures()` in the host app's `main.dart`
/// to enable the wallet (coins + dollar balances, transaction history).
library;

export 'core/wallet_feature.dart';
