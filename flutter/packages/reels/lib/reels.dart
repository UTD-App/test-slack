/// Reels feature package — public API.
///
/// Add `ReelsFeature()` to `buildFeatures()` in the host app's `main.dart`
/// to enable the reels (short-video) feed.
library;

export 'core/reels_feature.dart';
